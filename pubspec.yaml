name: dart_conf_flutter
description: A conference app for DartConf 2018.

dependencies:
  cloud_firestore: ^0.2.6
  cupertino_icons: ^0.1.1
  flutter:
    sdk: flutter
  http: ^0.11.3+16
  intl: ^0.15.2
  url_launcher: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/dartconf.png
    - assets/google_la_map.png
    - assets/app.token.txt
